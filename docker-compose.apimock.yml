version: "3"

services:

  frontend:
    depends_on:
      - apimock
      - nginx

  apimock:
    build:
      context: ./apimock
    image: apimock-audio_player
    container_name: apimock-audio_player
    hostname: apimock-audio_player
    command: npm run apimock:watch
    network_mode: "host"
    expose:
      - "8000"
    env_file:
      - .env
    volumes:
      - ./apimock/server:/apimock/server
